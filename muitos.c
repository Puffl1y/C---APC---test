#include <stdio.h>

int main(){
    int x, m;
    scanf("%d", &m)
    while (int i=0; i<9; i++)
    {
        scanf("%d", &x);
        if(i>m){
            maior = i;
        }
    }
    printf("%d\n", m)
    return 0;
}