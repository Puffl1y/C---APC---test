#include <stdio.h>

int mai n() {
    int n;
    scanf("%d", &n);
    l = 1;

    while (l <= n){
        if (n%2!=0) {
        printf("THUMS THUMS THUMS\n");
        }
        else {
        printf("THUMS THUMS THUMS THUMS\n");
        }
        l++;
    }
    return 0;
}